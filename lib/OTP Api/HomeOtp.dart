import 'package:flutter/material.dart';

class HomeOtp extends StatelessWidget {
  const HomeOtp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Home Otp")),
    );
  }
}
